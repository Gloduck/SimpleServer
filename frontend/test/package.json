{
  "name": "simple-server-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:browser": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
